import math
angulo = int(input('Digite o valor do angulo: '))
seno = math.sin(math.radians(angulo))
tangente = math.tan(math.radians(angulo))
coseno = math.cos(math.radians(angulo))
print(seno, tangente, coseno)